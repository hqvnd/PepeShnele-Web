# 🎭 PepeShnele - Event & Announcement Platform

Полнофункциональная платформа для событий и объявлений с социальными функциями.

## 📦 Структура проекта

```
PepeShnele-Web/
├── backend/                    # Node.js + Express + MongoDB API
│   ├── config/                # Конфигурация БД
│   ├── controllers/           # Контроллеры (бизнес-логика)
│   ├── middleware/            # Middleware (auth, validation, errors)
│   ├── models/                # Mongoose модели
│   ├── routes/                # API маршруты
│   ├── app.js                 # Express приложение
│   ├── server.js              # Запуск сервера
│   └── package.json           # Зависимости
│
└── frontend/                  # HTML + CSS + JS интерфейс
    ├── index.html            # Главная страница
    ├── css/                  # Стили
    ├── js/                   # JavaScript модули
    ├── pages/                # HTML страницы
    ├── assets/               # Изображения
    └── README.md             # Документация
```

## 🚀 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone <your-repo-url>
cd PepeShnele-Web
```

### 2. Настройте Backend

```bash
# Установите зависимости
npm install

# Создайте .env файл
cp .env.example .env

# Отредактируйте .env.example и добавьте:
MONGODB_URI=mongodb://localhost:27017/pepe-shnele
PORT=5000
NODE_ENV=development
JWT_SECRET=your_super_secret_key_here_change_this
JWT_EXPIRE=30d
CLIENT_URL=http://localhost:8000
```

### 3. Запустите MongoDB

```bash
# macOS (через Homebrew)
brew services start mongodb-community

# или напрямую
mongod

# Linux
sudo systemctl start mongod

# Windows
net start MongoDB
```

### 4. Запустите Backend

```bash
# Development режим (с auto-reload)
npm run dev

# Production режим
npm start
```

Backend будет доступен на: **http://localhost:5000**

### 5. Запустите Frontend

```bash
# Вариант 1: Python
python3 -m http.server 8000

# Вариант 2: Node.js
npx http-server -p 8000

# Вариант 3: VS Code Live Server
# Откройте frontend/index.html и нажмите "Go Live"
```

Frontend будет доступен на: **http://localhost:8000/frontend/index.html**

## 🎯 API Endpoints

### 🔐 Аутентификация (`/api/auth`)
- `POST /register` - Регистрация
- `POST /login` - Вход
- `GET /me` - Текущий пользователь

### 🎭 События (`/api/events`)
- `GET /` - Все события (фильтры: category, search, date)
- `GET /:id` - Одно событие
- `POST /` - Создать (admin)
- `PUT /:id` - Обновить (admin)
- `DELETE /:id` - Удалить (admin)
- `POST /:id/like` - Лайк/анлайк
- `POST /:id/comments` - Добавить комментарий
- `DELETE /:id/comments/:commentId` - Удалить комментарий
- `POST /:id/comments/:commentId/like` - Лайк комментария
- `POST /:id/rate` - Оценить событие

### 📢 Объявления (`/api/announcements`)
- `GET /` - Все объявления
- `GET /:id` - Одно объявление
- `POST /` - Создать (admin)
- `PUT /:id` - Обновить (admin)
- `DELETE /:id` - Удалить (admin)
- `POST /:id/like` - Лайк/анлайк

### 👤 Пользователи (`/api/users`)
- `GET /profile` - Профиль
- `PUT /profile` - Обновить профиль
- `POST /favorites/:eventId` - Добавить/удалить избранное
- `GET /favorites` - Список избранного

## 🎨 Frontend страницы

### Публичные
- **/** - Главная страница
- **/pages/events.html** - Все события
- **/pages/event-detail.html?id=...** - Детали события
- **/pages/announcements.html** - Объявления
- **/pages/login.html** - Вход
- **/pages/register.html** - Регистрация

### Защищенные
- **/pages/profile.html** - Профиль пользователя

### Админ
- **/pages/admin/create-event.html** - Создать/редактировать событие
- **/pages/admin/create-announcement.html** - Создать объявление

## 🔐 Создание админа

После регистрации пользователя, сделайте его админом через MongoDB:

```bash
# Войдите в MongoDB
mongosh

# Выберите БД
use pepe-shnele

# Найдите пользователя
db.users.find()

# Обновите роль на admin
db.users.updateOne(
  { email: "admin@example.com" },
  { $set: { role: "admin" } }
)
```

## 🛠️ Технологии

### Backend
- **Node.js** - Runtime
- **Express** - Web фреймворк
- **MongoDB** - База данных
- **Mongoose** - ODM
- **JWT** - Аутентификация
- **bcryptjs** - Хеширование паролей
- **CORS** - Кросс-доменные запросы

### Frontend
- **HTML5** - Разметка
- **CSS3** - Стили
- **JavaScript** - Логика 
- **Fetch API** - HTTP запросы

## 📝 Особенности

### Backend
✅ RESTful API  
✅ JWT аутентификация  
✅ Роли (user/admin)  
✅ Валидация данных  
✅ Обработка ошибок  
✅ Rate limiting  
✅ CORS настройки  

### Frontend
✅ Минималистичный дизайн  
✅ Полностью адаптивный  
✅ Единый CSS файл  
✅ Без сборщиков  
✅ Плавные анимации  
✅ Социальные функции (лайки, комментарии, рейтинги)  

## 🎯 Основные функции

- 📋 События с категориями (8 категорий)
- 🔍 Поиск и фильтрация
- ❤️ Лайки событий и объявлений
- 💬 Комментарии с лайками
- ⭐ Рейтинги (1-5 звезд) для прошедших событий
- 📌 Избранные события
- 📢 Объявления (с привязкой к событиям)
- 👤 Профили пользователей
- 🔐 Двухуровневая система доступа (user/admin)

## 📱 Категории событий

1. 📚 **Образование** - education
2. 🎭 **Развлечения** - entertainment
3. ⚽ **Спорт** - sports
4. 💻 **Технологии** - technology
5. 💼 **Бизнес** - business
6. 🎨 **Искусство** - arts
7. 🤝 **Социальное** - social
8. 📌 **Другое** - other

## 🧪 Тестирование API

### Используйте Postman или curl:

```bash
# Регистрация
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","email":"test@test.com","password":"123456"}'

# Вход
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"123456"}'

# Получить события
curl http://localhost:5000/api/events

# Создать событие (нужен токен админа)
curl -X POST http://localhost:5000/api/events \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN_HERE" \
  -d '{
    "title":"Test Event",
    "description":"This is a test event description with more than 20 characters",
    "category":"technology",
    "eventDate":"2026-12-31T18:00:00",
    "location":"Test Location"
  }'
```

## 🐛 Troubleshooting

### Backend не запускается
- Проверьте, что MongoDB запущен
- Убедитесь, что порт 5000 свободен
- Проверьте .env файл

### Frontend не работает
- Проверьте, что backend запущен
- Откройте консоль браузера (F12) для ошибок
- Проверьте URL в `frontend/js/api.js`

### CORS ошибки
- Убедитесь, что CLIENT_URL в .env соответствует адресу frontend
- Перезапустите backend после изменения .env

## 👥

Pepe Shnele - Alihan, Iskander, Daniyal

---

**Приятной работы! 🎭**
